// Bài 4: // in ra tổng các số có trong array
var arr2 = ['4', '6', 1,2,3,5, 'y', 't', 5]

let sum4 = 0;
for(let i = 0 ; i < arr2.length ; i++) {
    if(typeof arr2[i] === "number"){
    sum4 += arr2[i];
}
}
console.log(`Tổng các sô trong array trên là: ${sum4}`);