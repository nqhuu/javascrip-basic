// Bài 9:  
var array = [1,3,7,8,9,0,10,3,2];
// a.	áp dụng array.sort  sắp xếp array trên theo thứ tự tăng dần
// let arrtangdan = array.sort((a, b) => a - b);
// console.log(arrtangdan);

// b.	áp dụng array.map  , Tạo ra một array mới với các giá trị gấp 2 array cũ (Nó lấy luôn array của câu a sau khi đã sắp xếp thứ tự)
function arrmoi(arrnhan2) {
    return arrnhan2 *= 2
    ;
 }
 let kq = array.map(arrmoi);
 console.log(kq);

    // let arrmoi = array.map(elemen => elemen * 2);
    // console.log(arrmoi); // sai, nó lấy index để nhân

// c.	cho array3= [1,2,3,5,1] hãy thêm array3 vào phía trước của array và in ra kết quả . 
        array.unshift([1,2,3,5,1]);
        console.log(array);
// d.	cho array3= [1,2,3,5,1] hãy thêm array3 vào vị trí số 5 của array và in ra kết quả (ÁP dụng array.splice )
    let themarr = array.splice(5,0,[1,2,3,5,1]);
    console.log(themarr); // tại sao ko ra đáp án nhỉ

// e.	áp dụng array.filter lọc ra các phần tử lẻ , các phẩn tử chẵn .  

// f.	 Thêm số 5 vào giữa array .
